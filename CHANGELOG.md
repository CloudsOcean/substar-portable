# Changelog

## 1.0.13 — 2026-08-30

- AI 辅助切分支持选填参考稿：参考稿用于校正词元与最终显示标点，AI 仍依据无句界词级时间轴完成语义分组，不会被参考稿标点强制切断。
- 将“参考文稿辅助切分”明确拆分为“按参考稿标点切分”；该模式仍要求参考稿，并只按词元尾部的有效语义标点确定机械边界。
- 参考稿词元分析保护数字千分位、小数、版本号、时间、缩写、邮箱与网址，避免内部逗号、句点和冒号被误识别为字幕断点。
- AI 切分完成后恢复参考稿标点，同时保留替换与插入差异标记，且不改变 AI 已确定的 Cue 边界和时间。

## 1.0.12 — 2026-08-29

- 将 AI 辅助切分、AI 校准与字幕翻译统一为可核验的模型处理 `0/N`、Fallback `0/M`、结果验收、生成可编辑结果与交付产物阶段；进度只前进不回跳。
- 文本模型请求统一从当前服务商解析 Base URL、API Key、模型、思考模式与推理强度；GLM 5.3 在快速开始和全部 Stage 中固定采用受支持的“思考 + Low”，不再发送“关闭思考”。
- 校准与翻译改为先完成全部主批次，再只对失败块执行真正的 Fallback；修复请求携带被拒输出、程序校验错误与修复轮次，无法修复的内容保留可编辑文本并登记为问题字幕。
- 修复切分成功后进度事件引用未定义失败计数、导致 98% 处误判整项失败的问题；切分 Fallback 同样保留已接受块并仅修复失败块。
- 补充三类任务的失效块注入测试、六 Stage 服务商路由测试及 GLM 思考能力回归；真实案例完成 Qwen 听写增强注入、AI 切分、AI 校准和英译中全流程验收。

## 1.0.10 — 2026-08-28

- 修复 1.0.9 中语义切分 Worker 仍固定申请旧 `Segment_DeepSeek` 权限，导致已配置 GLM 时主切分与 fallback 在模型调用前失败的问题。
- 将文本模型密钥统一为服务商隔离的权限引用；切分、fallback、模型发现、思考能力测试与 Qwen 听写增强 AI 填写均解析当前服务商自己的密钥，不再借用其他服务商槽位。
- 项目创建任务会冻结服务商 ID、Base URL、认证方式和各 Stage 模型；官方端点与服务商身份不匹配时在排队前拒绝，避免错误路由或密钥误投。
- 兼容 1.0.9 以前没有服务商 ID 的失败任务：重试时按已冻结端点识别服务商，并保留无歧义的旧 DeepSeek 密钥迁移能力。
- 使用用户提供的失败工程形态补充 GLM 旧快照、fallback、凭据隔离、端点归属和错误提示回归测试。

## 1.0.9 — 2026-08-28

- 修复快速开始配置智谱后各 AI Stage 仍残留 DeepSeek 模型的问题；切分、切分修复、翻译、翻译修复、AI 校准与审计修复会随服务商原子切换。
- 增加任务冻结时的服务商与模型防御校验，旧项目快照不会再把明显不匹配的 DeepSeek/GLM 模型发送到另一方官方接口。
- 隔离各文本服务商的 API Key，修复配置智谱后 DeepSeek 被误报为已配置，并兼容清理旧版本产生的凭据别名污染。
- Qwen 听写增强的 AI 智能填写改为始终跟随当前文本服务商连接模型，同时保留 Prompt、临时热词、输入指纹和供应商提交审计链路。
- 补充 GLM/DeepSeek 双向切换、六 Stage 同步、凭据迁移、Qwen Prompt 与热词注入的回归测试。

## 1.0.8 — 2026-08-28

- 包含 1.0.7 的全部修复，并统一规范化项目创建与删除使用的 Windows 输出根目录，消除 CI 临时目录别名造成的删除校验误判。

## 1.0.7 — 2026-08-28

- 包含 1.0.6 的全部功能，并修复 Windows CI 中临时目录别名导致项目输出目录被误判为越界的问题，恢复自动构建与 GitHub Release 发布。

## 1.0.6 — 2026-08-28

- 重构文本模型服务配置：智谱 GLM 快速开始与完整模型列表可直接复用到各 Stage，连通性测试同时判断思考/非思考能力，推理强度统一为五档并按服务商映射。
- 新增 Qwen 听写增强：支持任务级 Prompt、临时多语言热词与 AI 智能填写；用户直接点名的术语按超级热词处理，模型优先非思考、不可用时回退到 Low 思考。
- 统一兼容 OpenAI 的模型响应解析；合法但正文为空的连通响应不再被误报，`reasoning_content` 在所有服务商上采用同一处理规则。
- 简化词库与审阅工作流：切分页暂时固定使用全局词库，移除内部 AI 审阅，外部 AI 审阅保持只读、可复制且不调用工程内模型。
- 修复切分修复链、超长问题字幕登记、孤儿任务恢复、后端断线状态与长日志横向滚动问题，保留失败后的人工处理入口。
- 完成切分页听写增强、上传区和快速开始界面整理，并修复初级/进阶教程定位及 AI 校准菜单残留问题。

## 1.0.5 — 2026-08-27

- 包含 1.0.4 的 GLM、智能吸附参数和设置页整合功能，并恢复 Windows 临时目录 8.3 路径展开时必须执行的规范化检查；1.0.4 的自动构建未产出 Release 附件。

## 1.0.4 — 2026-08-27

- 新增智谱 GLM 服务商配置与连通性测试，并按服务商声明的模型能力映射思考模式和 low / high / max 推理档位。
- 修复兼容 OpenAI 接口的模型响应编码与语义执行路径，避免对单个 API 打补丁或错误拒绝不支持关闭思考的模型。
- 智能向前吸附新增可保存的“前置静音余量”和“灵敏度”参数，默认值保持既有算法行为，并允许更积极地识别较弱起音。
- 将快捷键和高级运行资源设置合并进“通用”，保留旧地址兼容，并修复窄窗口下的横向溢出。
- 补充 GLM 能力映射、响应编码、语义执行和自动吸附参数的 Python / Node 回归测试，同步更新架构地图。

## 1.0.3 — 2026-08-27

- Cue 列表改用按 Cue ID 的增量协调：切分、合并、撤销和服务端确认只更新真实变化的行，不再重建滚动面板或改写滑块位置。
- 修复教程项目的普通刷新、浏览器重载和项目切换误触发教程重置的问题；只有明确重新启动教程才恢复案例基线。
- 智能向前吸附扩展为一秒自适应起音检测，并将首尾相接的非人工 Cue 作为共享边界移动；随后才按阈值向后填充剩余空隙。
- 参考稿变更图例改为无圆角裁切的标准实线与虚线，保持正文变更语义和颜色不变。
- 补充共享边界吸附、长列表增量重绘和教程刷新持久性的 Python/Node 回归测试，并同步更新可执行架构地图。

## 1.0.2 — 2026-08-27

- 编辑器常规操作不再意外驱动 Cue 列表、预览和时间轴跳转；三方联动收敛到 Cue、时间轴和播放定位等明确导航入口。
- 时间轴支持直接点击 Cue 块移动播放头，并提供单条/联动选择模式；结构编辑后保持当前工作位置稳定。
- 初级教程改用语义锚点适配新的切分、合并、参考稿选择和时间轴操作，不再依赖固定 Cue 序号。
- 翻译对所有目标语言统一采用软上限：非空超限译文保留并登记为问题字幕，空译文和结构不完整结果仍拒绝放行。
- AI 审阅状态、系统“另存为”导出及相关前端模块独立化，并补充 Python 与 Node 回归测试。
- 修复 AI 审阅响应被错误字符集解码后出现乱码的问题；损坏响应会被识别并拒绝进入问题清单。
- 初级教程扩展为 19 步：时间边界步骤会提示先移开遮挡边界的播放线，并新增 Alt＋滚轮缩放、空隙目标线和虚拟鼠标引导。
- 新建 Cue 教程接受任意非空文字；隐藏、恢复和删除按当前阶段动态定位并高亮真实操作按钮。
- 强化便携仓库的项目目录规范化及自包含发布门禁，正式包继续排除运行时用户数据、日志与凭据。
- 新增独立便携版源码仓库与 Windows 预览/正式发布流水线；运行时、用户项目、日志和凭据不进入源码提交。

## 1.0.1 — 2026-08-26

- 繁简功能改为非破坏性的显示与导出投影，不再批量改写双语正文；新增原文投影，并复用 OpenCC 转换器以近即时切换。
- 修复 Windows 上编辑器 AI 大请求在短轮询超时后被截断的问题，AI 校准与 AI 审阅现在会完整传输请求 JSON。
- 修复 Windows 正式便携包遗漏编辑器模型请求 Worker，恢复内置 AI 校准与 AI 审阅；全块失败不再伪报完成。
- 翻译并发块局部失败时保留已接受结果，将未解决 Cue 登记为问题字幕，不再使整项任务丢失。
- 修复初级教程注册与启动导引、进阶教程音频物化与波形恢复，并允许重新启动教程自动修复既有项目。
- 恢复编辑器折叠工具区、连续词元查找、智能吸附、说话人按钮锁定状态，以及默认导出目录和撤销/重做快捷键设置。
- 便携构建现在写入精确源码提交并显式校验全部生产 Worker，避免 `build-time` 身份冲突和不完整归档进入 Release。

## 1.0.0 — 2026-08-26

- 正式发布统一字幕生产工作流：ASR、AI 辅助切分、校准、翻译、审阅和编辑器操作共享可追踪的 75–100 秒工作分组。
- 内置 AI 线路默认使用 DeepSeek Flash low，并保留失败单元的有限回退与问题字幕标记；已接受的模型结果不会被程序重写。
- 外部 AI 工作包提供切分、校准、翻译各阶段的预制处理代码、JSON 检查点和 SRT 成品，支持衍生结果覆盖性导入。
- 加入固定的初级与进阶教程案例；教程项目按普通项目注册，支持重命名、编辑、导出和删除，进阶流程使用预存结果模拟 AI 调用。
- 强化编辑器 AI 任务取消、校准高亮、参考文稿边界、外部结果校验、恢复路径与 Windows 透明便携构建门禁。
- 本正式版不包含配音工作台及配音相关设置；相关开发继续保留在独立本地分支。

## 1.0.0-beta.6-preview.1 — 2026-08-23

- 新增结构化“外部 AI 校译”和“外部 AI 切分”往返流程：导出任务、校验预览、确认后原子提交，并拒绝过期修订、非法边界或会覆盖既有翻译的切分结果。
- 提示词组件可在设置页按族与路由查看、编辑和恢复；运行路由保持固定，不暴露为可修改配置。
- 完成参考文稿匹配与标点/断句工作流，补充手动 Cue、时间线编辑、项目任务信息和可审计变更来源。
- 强化编辑器交换、参考稿边界、翻译绑定、提示词目录和前端交互契约测试，并同步更新可执行系统架构映射。
- 修复透明便携构建的 OpenCC 运行时依赖、资源路径探针及受限环境 pytest 临时目录，恢复完整 Windows 发布门禁。
- GitHub 预览与正式发布改为安装固定测试依赖并执行与本地一致的 Python/Node 全量门禁，再从纯运行依赖组装便携运行时。

## 1.0.0-beta.5-preview — 2026-08-19

- Windows 包改为透明源码运行时目录：开发版与便携版执行逐文件一致的 Python、前端、Prompt、Schema 和 Worker。
- 删除 PyInstaller `_internal`、`sys.frozen`、`--backend` 与 `--worker-script` 双重执行路径。
- 固定 Python、依赖和 FFmpeg 统一收纳在 `runtime/`，构建时验证版本及生产文件 SHA-256。
- 凭据改为随完整 `data/` 迁移的 AES-GCM 信封；旧机器 DPAPI 文件仅作为一次性迁移来源。
- 加入透明目录 Launcher、语义切分素材和无用户数据门禁。

## 1.0.0-beta.4 — 2026-08-18

- 恢复开发版的有限交付语义：语义切分经过一次模型修复仍不合法时，仅对失败块生成确定性安全 Cue，并逐 Cue 标记为待人工审阅，不再令整个项目失败。
- 通过的模型块保持原样；降级过程不删除、不改写、不重排源词元，只补充满足显示硬上限的边界。
- 新增切分降级审计文件与回归测试，并将新项目默认切分修复次数恢复为一次。

## 1.0.0-beta.3 — 2026-08-18

- Replaced the implicit Markdown transcription-to-segmentation boundary with the strict `substar.segmentation-material.v1` JSON contract.
- Added a production producer-to-algorithm contract test and made local algorithm failures visible through a bounded, redacted task message.
- Kept source and portable builds on the same Worker command and material implementation.

## 1.0.0-beta.2 — 2026-08-18

- 修复冻结版语义切分把进程启动前缀误传给算法解析器、导致素材位置参数重复的问题。
- 切分算法失败时向任务页面投影经过截断和脱敏的错误末尾，不再只显示笼统的 Worker 失败。
- 冻结 Windows 便携版目录契约，并把架构图与目录说明纳入发行包。
- 发布压缩前清除 smoke import 生成的空数据目录，发行包不再携带用户态目录。

## 1.0.0-beta.1 — 2026-08-18

- 建立统一的持久任务运行时、Worker 协议、取消/重试/关机所有权与实例身份契约。
- 项目创建改为 `transcription -> segmentation -> editor-ready` 任务图。
- 听写生产路径收敛为 Qwen 云端；切分完成后直接进入编辑器。
- 翻译、AI 校准、AI 审阅改为编辑器独占任务，执行时锁定编辑。
- 密钥按用途命名并使用 Windows 机器范围 DPAPI：`ASR_Qwen`、`Segment_DeepSeek` 等。
- 删除旧项目兼容层、旧 ZIP 导入、本地 ASR、多路实验流水线和版本号残留命名。
- 编辑文档、修订、波形和项目列表公开契约统一为新版本 `v1`。
- Windows 包不再携带项目、用户数据或研究脚本。
