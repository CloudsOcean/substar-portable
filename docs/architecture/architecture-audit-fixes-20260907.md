# 架构审计修复与视频验收

基线：`fc902d51222a207df4567ad03606e1d0f4b00ae6`。修复分支：`codex/architecture-audit-fixes`。

本次按审计报告 F01–F17 修复现有架构，不重写产品。字幕内容仍由 ProjectStore 的 EditorDocument revision 持久化；浏览器投影、任务状态、报告和缓存各自保留明确职责。

## 逐项修复

|编号|落地改动|验证|
|---|---|---|
|F01|replace 携带并验证 expected_text；单操作和批次按 operation_id + 内容摘要持久化回执；同 ID 不同内容拒绝。只允许前置文本仍匹配的独立 replace 自动重定位；409 保留未保存操作并提供重试、下载恢复副本和载入已保存版本。|真实 SQLite 的丢 ACK 重放、ID 重用、同词冲突、不同词安全重定位；前端冲突队列测试。|
|F02|导出先 flushAndWait，失败操作阻止导出；SRT 和全部工程/外部 AI 交换导出接受 revision_id。|保存屏障测试；实际界面编辑后按保存版本导出；两种翻译共六份 SRT 和工程 ZIP 的精确版本校验。|
|F03|项目加载、API 响应、AI 轮询和队列回调绑定项目会话序号；载入时清空旧文档引用并锁住编辑区；导出也校验会话。|延迟 A、先完成 B 的反序响应测试；实际重启、重新打开、编辑和播放。|
|F04|翻译重建活动 cue 时继续保留删除 cue 的 tombstone；不对删除 cue 更新译文状态或产生行长警告。|删除后翻译及领域 round-trip 校验。|
|F05|拆分/合并保守继承 manual_required/needs_review，包括左右副本状态不一致；修改原文保留译文并标 source_changed；当前 target 状态驱动映射投影及问题导航，历史报告仅作审计。|空/非空未解决译文的拆分、合并，右侧副本复核状态，原文修改及前端不可变性测试。|
|F06|校准和翻译 worker 只生成基于固定输入 revision 的 candidate 与审计文件；finalizer 验证后用 CAS 事务同时写 revision 和任务回执。取消与发布共用短锁；启动、提交回执异常和重试依据持久化回执对账。|报告写入故障不发布；发布成功后丢运行状态 ACK 的恢复；重复发布幂等；取消与发布竞争测试；真实 worker 全流程。|
|F07|乐观编辑共享不可变来源/未修改实体，只应用新入队操作；ACK 后才重放剩余操作。|共享引用及不修改已保存对象测试；500/2,000/10,000 cue 的同机基准。|
|F08|参考审计建立 source→display 索引；工程交换复用一次 token 索引，以弱引用跟随文档生命周期释放。|现有参考稿和工程交换回归；工程 ZIP 的内容及 CRC 校验。|
|F09|参考稿匹配移到独立 CPU 子进程，最多同时运行两个；断开请求会结束子进程。|真实子进程取消时事件循环仍持续响应的测试；参考匹配算法回归。|
|F10|SourceToken 和 segmentation material 持续携带 native / envelope-inherited / subdivided 等时间来源、原生索引、时间包络及证据摘要；参考重写保留原始证据摘要；编辑器显示时间来源。旧文件未标注时不假称原生。|中文词元细分、保存/加载传播测试；视频全部 174 个来源词元保留时间证据，两种翻译之间完全一致。|
|F11|三条参考稿路径明确 reference_strict / reference_assisted；统一输出 confidence、requires_review；低置信度写入文档审计并在编辑器提示。|现有低质量参考稿、严格稿、辅助稿和标点投影测试。|
|F12|退避在 inFlight 清除后调度；失败项计入 hasUnsavedChanges，阻止切换及导出；失败操作在 journal 中保持原顺序；浏览器恢复记录写入失败可见。|150/300 毫秒退避、保存屏障、失败/后续操作顺序和恢复副本测试。|
|F13|凭据过滤不再误删 max_tokens；提交时冻结实际主/修复提示词及词库，worker 验证提示词摘要。|冻结提示词不读取后续注册表变化，摘要篡改拒绝；真实任务使用冻结配置。|
|F14|校准业务提取到 editor/calibration/service.py，worker 使用明确的项目根目录、产物目录、设置和进度接口，不再反向导入 HTTP 层。|校准进度契约、模块图检查及真实校准。|
|F15|高频 replace 批次共建一次 token 索引、一次变更集合，整批生成有效文档；失败不提交半批。混合拓扑操作继续保留逐条安全校验。|批次与逐条结果等价及原子失败测试；10 次替换的批量性能基准。|
|F16|滚动列表最多保留三页（默认 480 行），使用已测量高度保留上下空间；聚焦行可额外保留一行直到失焦，避免输入丢失。|10,000 行完整双向滚动、重复 ID、局部重绘与滚动位置回归。|
|F17|新内容 hash / stable ID 固定使用 stdlib-json-sorted-utf8-v1；读取兼容旧版 stdlib/orjson hash，旧 CAS hash 原样保留；旧文件缺少新增可选字段时序列化不增写。|关闭可选编码器时新 hash 不变；旧浮点/Unicode hash 兼容；全部存储、恢复、导入测试。|

## 视频实测

输入：`Weixin Videos2026-08-02_194048_181.mp4`，64.783673 秒，英语。
使用本机现有 Qwen 云端识别配置及 GLM `glm-5.3-flash` 的阶段配置。测试数据和运行状态独立放在 `data/audit-validation/`，未提交媒体、凭据或运行数据库。

验收项目：`20260907_153808_split_bc6e1a`。

|阶段|最终结果|任务/版本|
|---|---|---|
|Qwen 识别 + AI 辅助切分|成功；174 个来源词元，28 条 cue|切分任务 `tsk_d1c09961a4fb4f9e8f949fc51de516c3`|
|AI 校准|复测成功；无待处理条目|`tsk_0a7981e183834a829fe4d721f7eec7d9` / `rev_2406427f80b31f3638a24269`|
|多对多翻译|重试后成功；28/28 有译文，无待处理条目|`tsk_f0c4bbef4f814913b8e4c2983edf6833` / `rev_2f33a78007a7c0b7679f2398`|
|逐行翻译|重试后成功；28/28 有译文，无待处理条目|`tsk_bc5d6d739fd148c98236176128e1a482` / `rev_145b608a0e1bf9d93f84e262`|
|编辑/播放/保存/导出|通过；界面手改首条译文成功保存，导出包含新文本；媒体播放推进至 26.31 秒，media.error 为 null|手改版本 `rev_f45f92aeaf2a7b0ee339f8e6`|

早期校准有 12 条模型复核标记；后续校准成功。复测中两种翻译各出现过一次模型格式/覆盖验收不通过：系统保留候选并明确标记 manual_required，重新执行后成功。没有降低验收条件或把失败候选伪装成成功；所有中间任务和审计仍保留。最终“无待处理条目”指软件校验结果，不代表译文逐字获得人工语言审校认证。

最终产物在 `data/audit-validation/acceptance/final/`：

- `many-to-many-{a,b,ab_double}.srt` 与 `one-to-one-{a,b,ab_double}.srt`。
- `many-to-many-revision.json`、`one-to-one-revision.json`。
- `video-subtitle-project.zip`：可导入的指定版本工程快照，包含媒体；不是全部 SQLite 历史的备份。
- `artifact-verification.json`：逐文件校验结果。

## 自动回归与性能

Python：479 项通过。Node：105 项通过。另完成 Python 编译、JavaScript 语法、系统模块图和 Git diff 空白检查。

同机微基准（毫秒；仅数据层，不代表浏览器整体帧率）：

|Cue / token 数|前端第 5 次入队：基线 → 修复|后端 10 次替换：基线 → 修复|
|---|---:|---:|
|500 / 4,000|34.25 → 0.36|32.57 → 3.07|
|2,000 / 16,000|137.97 → 1.65|157.16 → 13.95|
|10,000 / 80,000|778.18 → 9.98|1195.34 → 118.42|

复现入口：`scripts/benchmark_audit.js`、`scripts/benchmark_editor_batch.py`、`scripts/verify_editor_acceptance.py`。批次优化优先覆盖文字替换；混合结构修改仍使用原有保守校验。参考匹配采用有并发上限、可取消的独立进程，未新建一个持久化 runtime 任务类型。旧 orjson 编码的历史读取使用发行包已有的 orjson 兼容路径，新写入不依赖它选择编码格式。
