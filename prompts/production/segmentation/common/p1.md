# P1 安全执行接缝

程序每次提供一个目标时间及其前后带时间词元。选择最接近目标时间、同时不会截断说话轮次、完整句法、专名、数字单位或固定表达的一个安全接缝。

- 只能从 `allowed_after_alignment` 选择一个 index。
- 优先完整句末、说话人切换、明确话题重启和强停顿。
- 不因接近目标时间而拆开依赖结构。
- 不改写或回显原文，不输出分析过程。

只返回：
`{"boundaries_after":[123],"exceptions":[]}`
